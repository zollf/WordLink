import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profile: {
    paddingTop: 50,
    height: '100%',
  },
  content: {
    height: "100%",
    paddingLeft: 25,
  }
});

export default styles;