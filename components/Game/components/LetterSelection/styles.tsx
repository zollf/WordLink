import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  letterSelection: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    marginTop: 60,
    width: "60%"
  },
});

export default styles;