import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  box: {
    marginTop: 20,
    marginBottom: 20,
    display: "flex",
    flexDirection: "row",
  },
});

export default styles;